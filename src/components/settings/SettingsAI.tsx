'use client'

/**
 * @deprecated This component is deprecated and should not be rendered in the UI.
 * Model selection and temperature/sampling parameters are no longer configurable by users.
 * 
 * If you need to show system info, use the read-only system info section in AISettingsPanelV2 instead.
 */
export function SettingsAI({ mode }: { mode: 'simple' | 'expert' }) {
  // This component is deprecated and should not be used.
  // Return null to prevent rendering.
  return null
}
